// Fallback branding used wherever no tenant-scoped app settings are available yet
// (e.g. the login/register screens, which render before a tenant is known).
// Once authenticated, tenant app settings (app_name / browser_tab_title) take over.
export const DEFAULT_APP_NAME = 'SpaceHub';
export const DEFAULT_BROWSER_TAB_TITLE = 'SpaceHub — Διαχείριση Πελατών';
